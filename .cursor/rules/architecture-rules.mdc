---
description: Regras de arquitetura e design system que devem sempre ser respeitadas
globs: ["**/*.{js,css,html}"]
alwaysApply: true
---

# 🏛️ Arquitetura e Design System (SEMPRE Aplicados)

## Design Tokens - Paleta Arquitetônica
- **SEMPRE** use apenas cores da paleta oficial:
  - Moss: #545943 (verde arquitetura)
  - Beige: #E8DACB (tom neutro)
  - Terracotta: #B66C48 (tom terra)
- **SEMPRE** use variáveis CSS para todas as cores
- **NUNCA** use valores hex hard-coded no código

## Tipografia - Sistema Consistente
- **SEMPRE** use apenas fontes oficiais:
  - Inter (sans-serif) para textos gerais
  - Playfair Display (serif) para títulos
- **SEMPRE** use rem para tamanhos de fonte
- **SEMPRE** mantenha hierarquia tipográfica consistente

## Espaçamento - Sistema de 8px
- **SEMPRE** use múltiplos de 0.5rem (4px) para espaçamento
- **SEMPRE** mantenha consistência no ritmo vertical
- **NUNCA** use valores arbitrários de margin/padding

## Component Pattern - Classe Consistente
```javascript
/**
 * Padrão OBRIGATÓRIO para todos os componentes
 */
class ComponentName {
  constructor(selector) {
    this.element = document.querySelector(selector);
    this.init();
  }

  init() {
    this.cacheElements();
    this.bindEvents();
  }

  cacheElements() {
    // Cache OBRIGATÓRIO de elementos DOM
    this.button = this.element.querySelector('.component__button');
  }

  bindEvents() {
    // Bind com throttle/debounce quando apropriado
    this.button?.addEventListener('click', () => this.handleClick());
  }

  handleClick() {
    // Lógica do componente
  }
}
```

## Breakpoints - Mobile-First
```css
/* SEMPRE siga mobile-first */
/* Base: < 768px (mobile) */
/* Tablet: >= 768px */
/* Desktop: >= 1024px */
/* Large: >= 1280px */
/* Extra Large: >= 1536px */
```

## Performance Targets - Métricas Críticas
- **SEMPRE** mantenha LCP < 2.5s
- **SEMPRE** mantenha FID < 100ms
- **SEMPRE** mantenha CLS < 0.1
- **SEMPRE** execute `npm run analyze` regularmente